# 验证记录 · 2026-09-09

- `npm run catalog` 与 `npm run check`：34 条有效记录（5 GLB、4 web、25 images）。
- 5 个 GLB 的分片拼接 SHA256 均与研究目录中的原始文件相同。发布文件最大 48 MiB；不含依赖和临时构建的目录约 602 MiB。
- 浏览器实际加载全部 5 个 GLB，包括 119.7 MB 的参考图拟合版；已检查旋转、复位、下载链接出现及实际模型显示。
- 在 `/model-gallery/` 子目录下实际打开 4 个网页成果；两份心脏与线粒体显示已就绪，人体图谱完整几何显示。历史图片入口可打开。
- 三个程序化网页从保留的源码运行 TypeScript 检查与 Vite 构建成功；人体图谱从源码执行 `npm ci`、Vite 构建和 TypeScript 检查成功。
- 代码审查发现的图谱绝对路径、缺少源码配置/布局、Vite 静态目录配置问题已修复；两份心脏维护源码的 lockfile 已补齐。
- 原始研究模型和源码未修改。2026-09-09 已推送到 `rikka314/3d-research-`，Pages 配置为 `main` 根目录。发布前检查 475 个跟踪文件，未发现常见凭据模式；依赖目录和临时构建均不在提交中。
- 尚未在实体手机上验证。大模型的首次下载受网络和设备内存影响。

## 九月十一日研究成果导入

输出目录一百三十七个模型与导入清单逐一对应，两次完整运行的复制文件拼接摘要均与来源一致。全站一百七十一项标题唯一且不含英文字母。清单检查及界面、导入脚本语法检查通过，独立代码审查通过。浏览器已实际加载原有参考模型及新冠状窦候选的两片完整模型，并出现下载入口。未逐个重新渲染所有模型；原研究内容及验收状态不变，实验仍暂停。
